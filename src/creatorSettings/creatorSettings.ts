import { CreatorSettings } from "./creatorSettings.type";

const defaultSettings: CreatorSettings = {
  plugins: ["general", "css", "reactJS", "materialUI", "chakraUI", "builderIO"],
};

export default defaultSettings;
