export interface I{{general.answers.componentName}} {
  name: string;
}

export type Props = {
{{#if reactJS.answers.propList }}
{{#each reactJS.answers.propList}}
  {{ this }}?: string;
{{/each}}
{{else}}
  text?: string;
{{/if}}
};

export const defaultProps: Props = {
{{#if reactJS.answers.propList }}
{{#each reactJS.answers.propList}}
  {{ this }}: "",
{{/each}}
{{else}}
  text: "hello hayde",
{{/if}}
};
