import { Answers, Question } from "inquirer";
import { IPluginOptions } from "./interfaces";

export const qSupport{{ createPlugin.answers.pluginName }}: Question<Answers> = {
  when: (answers: IPluginOptions) => answers.testOption == undefined,
  type: "confirm",
  name: "testOption",
  message: "test question?",
  default: true,
};

export const questions: Question[] = [qSupport{{ createPlugin.answers.pluginName }}];
